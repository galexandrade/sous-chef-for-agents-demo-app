import { Page } from '@7shifts/sous-chef';

const Payroll = () => {
    return (
        <Page title="Payroll" subtitle="Payroll summary">
            {/* Build me a payroll summary view using Sous Chef components */}
        </Page>
    );
};

export default Payroll;
