import { Page } from '@7shifts/sous-chef';

const Employees = () => {
    return (
        <Page title="Employees" subtitle="Manage your team">
            {/* Build me an employee list view using Sous Chef components */}
        </Page>
    );
};

export default Employees;
